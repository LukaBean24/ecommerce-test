import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#222831',
      light: '#343d4b',
      dark: '#15181e',
      contrastText: '#fcda05',
    },
    secondary: {
      main: '#fcda05',
    },
  },
})

export default theme
